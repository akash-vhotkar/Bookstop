const profile = (state = {profiledata: null} ,  action )=>{
    switch (action.type) {
        case "profile":
            return state;
    
        default:
            return state
    }

}
export default profile